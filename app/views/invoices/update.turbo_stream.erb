<%= turbo_stream.update "invoice_errors", partial: "shared/form_errors", locals: { record: @invoice } %>
<% if @invoice.errors.empty? %>
  <%= render "invoices/totals", invoice: @invoice %>
<% end %>
