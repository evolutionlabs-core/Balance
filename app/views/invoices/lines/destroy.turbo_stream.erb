<%= turbo_stream.remove @line %>
<%= render "invoices/totals", invoice: @invoice %>
<% if @invoice.invoice_lines.empty? %>
  <%= turbo_stream.append "invoice_lines", partial: "invoices/line", locals: { invoice: @invoice, line: InvoiceLine.new } %>
<% end %>
