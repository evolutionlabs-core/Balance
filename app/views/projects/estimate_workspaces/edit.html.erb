<%= render "shared/modal" do %>
  <%= render "workspaces/form", workspace: @workspace, url: project_project_estimate_workspace_path(@project, estimate_id: (@estimate.id if @estimate.persisted?)) %>
<% end %>
